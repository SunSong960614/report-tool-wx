[project]
name = "report-tool-wx"
version = "1.0.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "Flask==3.1.2",
    "Pillow==11.3.0",
    "python-docx==1.2.0",
]

[tool.vercel]
entrypoint = "app:app"
